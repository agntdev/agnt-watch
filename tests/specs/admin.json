[
  {
    "name": "/admin from non-owner shows restricted message",
    "steps": [
      {
        "send": { "text": "/admin" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "This command is restricted to the bot owner." }
          }
        ]
      }
    ]
  }
]
